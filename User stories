AS [] I want to [] so that []
Given, when, then

As a visitor I want to be able to view a landing page that introduces Wavelength Records so that I can understand their history as a Seattle based indie label.

    Given a user enters the url for the website,
    When the page loads, 
    Then the user must see the label's name, their location, and their founding year.

    Given a user wants to learn more about wavelength records,
    When they click the about button in the header,
    Then the page must display a mission statement describing Wavelengths indie roster.

    Given a mobile user visits the landing page,
    When the screen width is 480px or less,
    Then the header, footer and main content must stack vertically without requiring horizontal scrolling. 

As a visitor I want to be able to find a specific artist and view their details so that I can see their genre and discography. 

    Given a mobile user navigates to the artists catalog,
    When the page loads,
    Then the user must see exactly five distinct artist profiles displayed in a grid or list. 

    Given a visitor is viewing the catalog,
    When they select a specific artist,
    Then they should be able to view the artists name, genre, a short biography and a list of their released albums. 

    Given a visitor navigates to the artist/catalog,
    When they view the artists,
    Then the layout must display the artists name and genre clearly over or under the artists photo. 
    